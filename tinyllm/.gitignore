# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/

# Model artifacts (large binaries; regenerable by training, and the weights are
# the crown-jewel IP -- not published)

# Unrelated working documents that happen to live in this directory
*.docx
*.pptx
~$*

# Real customer EBS extractions and one-off extraction tooling/logs -- these
# describe a specific customer's actual production schema; never published
# (matches the product's own "schema never leaves the customer network" design)
schema*.json
*.jsonl
extract*.log
other_source_check.log
# Enumerating individual log names kept missing new ones (apps_phase_*.log,
# train_ap_gl.log were both still unignored) -- ignore the whole class.
*.log
local_ops/
SESSION_NOTES.md

# OS / editor
.DS_Store
